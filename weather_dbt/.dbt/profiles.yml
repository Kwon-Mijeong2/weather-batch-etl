weather_dbt:
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      user: airflow
      password: airflow
      dbname: airflow
      schema: public
      threads: 1
  target: dev